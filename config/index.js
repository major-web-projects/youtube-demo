const config = {
  siteUrl: process.env.NEXT_PUBLIC_APP_SITE_URL || "http://localhost:3000",
  apiURL: process.env.NEXT_PUBLIC_APP_API_URL || "http://localhost:8000",
  apiUrl: process.env.NEXT_PUBLIC_APP_API_URL || "http://localhost:8000",
};

export default config;
